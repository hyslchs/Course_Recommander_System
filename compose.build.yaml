services:
  crs-app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CLIENT_BUILD_SHA: ${CLIENT_BUILD_SHA:-unknown}
      additional_contexts:
        crs_artifacts: ${CRS_ARTIFACT_BUNDLE_DIR:?Set CRS_ARTIFACT_BUNDLE_DIR to an immutable artifact bundle directory}
